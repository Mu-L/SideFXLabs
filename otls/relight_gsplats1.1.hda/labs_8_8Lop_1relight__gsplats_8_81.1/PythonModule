def getCameras(kwargs):
    import loputils
    lop = kwargs.get("node").node("warning")
    cams = loputils.globPrims(lop, '%type:Camera')
    return [x.GetPrimPath().pathString for x in cams for y in range(2)]